export type DropdownOption = {
  value: number;
  label: string;
}

export type BaseType = {
  ID: number;
  title: string;
  desc: string;
  CreatedAt: string;
  UpdatedAt: string;
  DeletedAt: string;
}
